<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" 
    android:background="@drawable/background_no_stripes">

    <EditText
        android:id="@+id/phoneEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="64dp"
        android:textColor="@color/black"
        android:ems="10"
        android:maxLength="10"
        android:inputType="number" >
        
        <requestFocus />
        
        </EditText>

    <TextView
        android:id="@+id/phoneTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/phoneEditText"
        android:layout_alignBottom="@+id/phoneEditText"
        android:layout_alignParentLeft="true"
        android:textColor="@color/black"
        android:text="Phone:"
        android:textAppearance="?android:attr/textAppearanceMedium" />
         <ImageButton android:layout_width="155dp" 
         android:id="@+id/loginButton" 
         android:src="@drawable/login" 
         android:layout_height="70dp" 
         android:layout_below="@+id/phoneEditText" 
         android:layout_alignLeft="@+id/phoneEditText" 
         android:layout_marginTop="82dp"></ImageButton>

</RelativeLayout>
